{
    "version": 2,
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/static-build",
            "config": {
                "buildCommand": "bash deploy.sh && echo 'Build completed'"
            }
        }
    ]
} 